# ============================================
# FIX: GPT Quant V9 vs V9.1 Comparison
# ============================================

# 修改檔案：
# .github/workflows/gpt-quant-v9-v91-comparison.yml

# 找到：

v9_metrics_path:
  description: "V9 JSON or CSV metrics path"
  required: true
  default: "artifacts/backtest/v9_metrics.json"
  type: string

v91_metrics_path:
  description: "V9.1 JSON or CSV metrics path"
  required: true
  default: "artifacts/backtest/v91_metrics.json"
  type: string

# 改成：

v9_metrics_path:
  description: "V9 JSON or CSV metrics path"
  required: true
  default: "backtest/examples/v9_metrics.example.json"
  type: string

v91_metrics_path:
  description: "V9.1 JSON or CSV metrics path"
  required: true
  default: "backtest/examples/v91_metrics.example.json"
  type: string

# ============================================
# Git
# ============================================

git add .github/workflows/gpt-quant-v9-v91-comparison.yml

git commit -m "Fix comparison workflow default metrics paths"

git push origin main
