import { useEffect, useMemo, useState } from "react";
import { loadLatestSignals, marketIntelligence, optimizePortfolio, regimeLabel } from "../lib/v9Data";
import type { SignalRow } from "../types/v9";

export default function ExecutionPlanV10(){ const [signals,setSignals]=useState<SignalRow[]>([]); useEffect(()=>{loadLatestSignals().then(setSignals).catch(()=>setSignals([]));},[]); const market=useMemo(()=>marketIntelligence(signals),[signals]); const portfolio=useMemo(()=>optimizePortfolio(signals,6),[signals]); const cash=market.regime === "RISK_OFF" ? 45 : market.regime === "NEUTRAL" ? 30 : 15; return <section><div className="page-title"><div><div className="eyebrow">V10 EXECUTION OS</div><h1>交易執行計畫</h1><p>將市場狀態、風險預算與候選標的轉換成每日執行清單。</p></div></div><div className="cards"><div className="metric"><span>市場狀態</span><strong>{regimeLabel(market.regime)}</strong><small>決定總曝險</small></div><div className="metric"><span>建議現金</span><strong>{cash}%</strong><small>保留風險緩衝</small></div><div className="metric"><span>候選持股</span><strong>{portfolio.length}</strong><small>最多 6 檔</small></div><div className="metric"><span>執行節奏</span><strong>{market.regime === "RISK_ON" ? "分批進場" : market.regime === "NEUTRAL" ? "等待確認" : "防守減碼"}</strong><small>依市場健康度調整</small></div></div><div className="panel"><div className="panel-title">今日執行清單</div><div className="allocation-list">{portfolio.map((row,index)=><div className="allocation-row" key={row.symbol}><div><strong>{index+1}. {row.symbol}</strong><small>{row.name ?? ""}</small></div><div className="allocation-bar"><i style={{width:`${Math.min(100,row.weight*100)}%`}}/></div><b>{(row.weight*100).toFixed(1)}%</b></div>)}</div></div></section>; }
