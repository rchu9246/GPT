import { useEffect, useState } from "react";
import { formatNum, formatPct, loadBacktestRuns, strategyScorecards } from "../lib/v9Data";
import type { StrategyScorecard } from "../types/v9";

export default function MultiStrategyV10(){ const [runs,setRuns]=useState<StrategyScorecard[]>([]); useEffect(()=>{loadBacktestRuns().then((rows)=>setRuns(strategyScorecards(rows))).catch(()=>setRuns([]));},[]); return <section><div className="page-title"><div><div className="eyebrow">V10 MULTI-STRATEGY ENGINE</div><h1>多策略比較</h1><p>以報酬、回撤、Sharpe 與勝率建立綜合策略評分。</p></div></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>策略</th><th>綜合分數</th><th>穩定度</th><th>總報酬</th><th>最大回撤</th><th>Sharpe</th><th>勝率</th></tr></thead><tbody>{runs.map((run)=><tr key={run.id}><td><strong>{run.strategy_version}</strong></td><td>{formatNum(run.compositeScore)}</td><td><span className={`rating ${run.stabilityLabel === "穩健" ? "rating-buy" : run.stabilityLabel === "中性" ? "rating-watch" : "rating-avoid"}`}>{run.stabilityLabel}</span></td><td>{formatPct(run.total_return)}</td><td>{formatPct(run.max_drawdown)}</td><td>{formatNum(run.sharpe_ratio,2)}</td><td>{formatPct(run.win_rate)}</td></tr>)}{!runs.length&&<tr><td colSpan={7}>目前尚無回測紀錄</td></tr>}</tbody></table></div></div></section>; }
