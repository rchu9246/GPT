import {supabase} from "./supabase";import type {Dashboard46} from "../types/enterprise46";export async function loadEnterprise46(){if(!supabase)return{dashboard:null as Dashboard46|null};const {data,error}=await supabase.from("enterprise_dashboard_v46").select("*").order("dashboard_date",{ascending:false}).limit(1).maybeSingle();if(error)throw error;return{dashboard:data as Dashboard46|null};}