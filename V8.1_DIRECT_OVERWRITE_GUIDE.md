# GPT Quant V8.1 Professional 直接覆蓋說明

1. 完整解壓縮 ZIP。
2. GitHub Desktop 點 `Repository → Show in Explorer`。
3. 將解壓後資料夾內的全部內容貼到 `GPT` Repository 根目錄。
4. 選擇「取代目的地中的檔案」。
5. 為避免舊檔混入，請刪除 `src/pages` 中任何非 `*V8.tsx` 的舊頁面；本 ZIP 本身已是乾淨版。
6. GitHub Desktop Summary：`Upgrade to GPT Quant V8.1 Professional`。
7. Commit to main → Push origin。
8. 等 GitHub Actions 成功，再按 `Ctrl + Shift + R`。
