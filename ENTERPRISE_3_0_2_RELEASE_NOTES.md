
# GPT Quant Enterprise 3.0.2 Stable

## Changes
- Node 24 compatible
- Replaced `npm ci` with `npm install` in validation workflow
- Standardized Python dependency installation
- Ready for GitHub Actions
