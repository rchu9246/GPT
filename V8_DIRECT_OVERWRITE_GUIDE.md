# GPT Quant V8 Professional 完整專案版

此 ZIP 不需要執行 BAT 或 PowerShell，可直接覆蓋 GitHub Repository。

## 操作

1. 完整解壓縮此 ZIP。
2. 在 GitHub Desktop 點 `Repository → Show in Explorer`。
3. 開啟解壓後的 V8 資料夾。
4. 按 `Ctrl + A`、`Ctrl + C`。
5. 貼到 GitHub Desktop 打開的 `GPT` 資料夾。
6. 選擇「取代目的地中的檔案」。
7. 回到 GitHub Desktop。
8. Summary 輸入：`Upgrade to GPT Quant V8 Professional`
9. 按 `Commit to main`。
10. 按 `Push origin`。
11. 等 GitHub Actions 變綠。
12. 網站按 `Ctrl + Shift + R`。

注意：複製的是解壓資料夾「裡面的內容」，不要把外層資料夾整個放進 GPT。
